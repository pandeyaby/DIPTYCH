operator: SCHEMAX
coupling: open_loop
horizon:
  unit: steps
  length: 8
description: |
  Perturb discrete schedule/schema key set
inconclusive_when:
  - traces incomparable under shared prefix rule
  - missing twin role
