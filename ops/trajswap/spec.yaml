operator: TRAJSWAP
coupling: crn_closed_loop
horizon:
  unit: steps
  length: 8
description: |
  Swap trajectory segments under CRN closed loop
inconclusive_when:
  - traces incomparable under shared prefix rule
  - missing twin role
  - crn_stream_id mismatch
